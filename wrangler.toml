# إعدادات Cloudflare Worker للبروكسي
name = "ai-chat-proxy"
main = "workers/proxy.ts"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "production"

# [secrets]
# ENCRYPTION_KEY - set via wrangler secret put
